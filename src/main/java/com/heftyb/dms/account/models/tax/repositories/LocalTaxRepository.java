package com.heftyb.dms.account.models.tax.repositories;

import com.heftyb.dms.account.models.tax.LocalTax;
import org.springframework.data.repository.CrudRepository;

public interface LocalTaxRepository extends CrudRepository<LocalTax, Long> {
}
