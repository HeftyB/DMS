package com.heftyb.dms.crm.repositories;

import com.heftyb.dms.crm.SaleLead;
import org.springframework.data.repository.CrudRepository;

public interface SaleLeadRepository extends CrudRepository<SaleLead, Long> {
}
